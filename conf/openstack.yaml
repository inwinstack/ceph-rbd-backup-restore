# sample
default:
    auth_url: http://0.0.0.0:35357/v2.0/
    tenant_name: admin
    user_name: admin
    password: password
    cacert_path: /etc/ssl/certs/ca-certificates.crt
    volume_names:
        -  volume_name
